export interface Hotel{
    id: string; // código de habilitación
    nombre: string;
    capacidad: number; // capacidad de reservas
    telefono: string;
    correo: string;
    habitaciones: number;  
}